{
  "name": "middle-east-history-mastery",
  "version": "1.0.0",
  "description": "Edexcel GCSE History Paper 2 Conflict in the Middle East Mastery",
  "main": "index.html",
  "scripts": {
    "build:js": "powershell -ExecutionPolicy Bypass -File build.ps1",
    "build:copy": "node build_assets.js",
    "build": "npm run build:js && npm run build:copy",
    "cap:sync": "npx cap sync",
    "build:apk": "cd android && gradlew.bat assembleDebug",
    "build:aab": "cd android && gradlew.bat bundleRelease"
  },
  "keywords": [
    "education",
    "history",
    "capacitor",
    "android"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/core": "^8.3.4"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4"
  }
}
